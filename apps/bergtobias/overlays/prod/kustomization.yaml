resources:
  - ../../base
namespace: bergtobias-prod
namePrefix: prod-
patches:
  - target:
      kind: Service
      name: bergtobias-service
    patch: |
      apiVersion: v1
      kind: Service

      spec:
        ports:
          - port: 80
            targetPort: 80
            nodePort: 30036
        type: NodePort
