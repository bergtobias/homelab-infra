apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: backstage
subjects:
  - kind: ServiceAccount
    name: backstage
    namespace: default
roleRef:
  kind: ClusterRole
  name: backstage
  apiGroup: rbac.authorization.k8s.io
