apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: app1
spec:
  parentRefs:
    - name: prod-web
  hostnames:
    - app1.localhost

  rules:
    - backendRefs:
        - name: app1
          port: 8080
