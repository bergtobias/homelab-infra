apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: app1
spec:
  parentRefs:
    - name: prod-web
      namespace: istio-system
  hostnames:
    - app.bergtobias.com
  rules:
    - backendRefs:
        - name: app1
          port: 80
