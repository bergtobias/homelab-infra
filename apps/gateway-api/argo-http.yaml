apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argo-http
  namespace: argocd
spec:
  parentRefs:
    - name: gateway
  hostnames:
    - "argo.localhost"
  rules:
    - backendRefs:
        - name: argocd-server
          port: 80
