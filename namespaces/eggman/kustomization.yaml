apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
helmCharts:
  - name: namespace-chart
    repo: file://../../charts/namespace-chart # ← lokal path!
    releaseName: eggman
    valuesInline:
      namespace:
        name: dev
        labels:
          environment: dev
          team: devops
      apps:
        - name: eggman
          repo: https://github.com/bergtobias/homelab-infra
          path: apps/eggman
          targetRevision: main
